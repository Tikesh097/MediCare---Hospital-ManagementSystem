# Frontend environment variables (Vite uses VITE_ prefix)
# The API proxy in vite.config.js handles the backend URL in development
# For production deployment, set the full backend URL:
VITE_API_URL=http://localhost:5000/api
